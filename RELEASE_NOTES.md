把資料夾裡文件中的**關鍵字**全部換成 `snoopy`，並以**原本的檔案格式**另存到 `snoopy_folder`。原始檔案永遠不會被修改。

*Replace a keyword with `snoopy` across a folder of documents, writing each result back in its original file format.*

## 下載

**KeywordReplacer.exe** — 單一執行檔，免安裝。需要 Windows 10 或 11（64 位元）。

> **⚠️ 第一次執行會被 SmartScreen 擋下來**
> 因為這個執行檔沒有購買程式碼簽章憑證。點「其他資訊」→「仍要執行」即可。
> 不放心的話，可以用 `SHA256SUMS.txt` 核對檔案雜湊。

## 支援格式

| 類型 | 副檔名 |
|---|---|
| 純文字 | `.txt`、`.csv`（保留原編碼、BOM 與換行符） |
| Office | `.docx`、`.xlsx`、`.doc`、`.xls` |
| PDF | 文字層、掃描影像、以及「看得到字卻抽不出字」的檔案 |
| 影像 | `.png`、`.jpg`、`.bmp`、`.tif` |

## v0.1.5 的變更

**修好含空格的英文關鍵字**。例如 `Monthly Statement`——OCR 是逐「詞」回報的，
舊版把同一行的詞接起來時沒有插入空格，變成 `MonthlyStatement`，於是你輸入的空格永遠比對不到。
中文不受影響是因為中文詞之間本來就沒有空格。

**OCR 信心度門檻可調**：GUI 多了一個欄位（預設 70），CLI 是 `--min-confidence`。
圖片模糊或解析度低導致辨識信心不足時可以調低。

## v0.1.4 的變更

**修好了「銀行月結單換不掉」**。v0.1.3 的偵測看的是造字區與亂碼符號，
但實際的月結單抽出來是**控制字元與未定義碼位**（1855 個字元裡佔九成），兩個訊號都沒踩到。

現在改用 Unicode 類別比例判斷，實測那份月結單可以正確取代。

**只有讀出來是亂碼的頁面才會重跑 OCR**：文字讀得出來卻沒有這個詞，就是真的沒有，
不會再花好幾分鐘整份重掃。報表訊息一律寫明試過哪幾級。

## v0.1.2 / v0.1.3 的變更

**OCR 引擎已內建**。過去掃描 PDF 與圖片要另外安裝 Tesseract，現在執行檔直接帶著
Tesseract 與 `eng` / `chi_tra` 語言資料，下載就能用。

代價是檔案從 43.7 MB 變成 **106.5 MB**。已經挑掉用不到的部分（ICU 資料表 31.6 MB、
方向偵測資料 10.1 MB），剩下的主要是 Tesseract 的 C++ runtime，沒得再省。

CI 會在移除系統 Tesseract 的 PATH 後實跑一次執行檔並檢查報表，確認用的真的是內建那份——
內建壞掉的版本發不出去。

**舊版 `.doc` / `.xls` 仍需自行安裝 [LibreOffice](https://www.libreoffice.org/)**——
它超過 700 MB，不適合隨附。

## v0.1.1 的變更

**PDF 改為三級自動升級**：文字層 → 內嵌圖片 OCR → **整頁 OCR**。

有些 PDF（銀行月結單特別常見）畫面上看得到字，程式卻抽不出正確字碼——嵌入字型缺 `ToUnicode`，
或整段文字根本是向量外框。舊版會以為「有文字層」而走錯路徑，結果一個字都沒換。
現在會自動偵測並升級到整頁 OCR，不需要你做任何事。

**沒換到一定會告訴你**：報表訊息會寫明試過哪幾級，不再默默給你一份沒改的檔。

## 設計原則

- **原始檔永不修改**，一律唯讀開啟。
- **副檔名進出一致**：`.doc` 出來還是 `.doc`，不會被順手升級成 `.docx`。
- **只動關鍵字**：字型、字級、顏色、欄寬、公式、圖片、頁首頁尾都不變。
- **做不到就不輸出**：處理不了的檔案記為 SKIP 並寫明原因，不會給你一份沒改到的檔。
- **全程離線**：文件內容不會離開你的電腦。

## 外部相依

- **OCR** — 已內建，不必安裝任何東西。
- **舊版 `.doc` / `.xls`** — 需要 [LibreOffice](https://www.libreoffice.org/)；未安裝時其餘格式照常處理，報表會標明。

隨附元件的授權見 `THIRD_PARTY_NOTICES.md`。

## 執行結果

每次執行會在 `snoopy_folder` 產生 `_report.csv`，逐檔列出來源、輸出、狀態（OK / SKIP / FAIL）、取代次數與訊息。
